name: Tests
on:
  push:
    branches: [main]
  pull_request:
jobs:
  required:
    runs-on: ubuntu-latest
    name: ubuntu / stable
    steps:
      - uses: actions/checkout@v5
      - name: Tests
        run: cargo test -v --no-fail-fast
